import '../globals.css';
const DashboardLayout = ({children}) => {
    return (
        <html>
            <body>
                <h1>Dashboard Layout</h1>
                {children}
            </body>
        </html>
    );
};

export default DashboardLayout;